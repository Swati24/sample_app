
<ul class="nav">
  <li><%= link_to "Take me Home", root_path, :class => 'brand' %></li>
  <% if user_signed_in? %>
    <li><%= link_to "Articles to be published", drafts_articles_path %></li>
    <% if current_user.role? :admin %>
      <li><%= link_to 'Create New Editor', new_editor_users_path %></li>
      <li><%= link_to 'All Editors', editors_path %></li>
    <% end %>
  <% end %>
</ul>

<ul class="nav pull-right">
  <% if user_signed_in? %>
    <li class="dropdown ">
      <a class="dropdown-toggle" data-toggle="dropdown" href="#" class="right-text"><%= current_user.name %><b class="caret"></b></a>
      <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
        <li><%= link_to "Settings", edit_user_registration_path %></li>
      </ul>
    </li>
    <li><%= link_to 'Logout', destroy_user_session_path, method: 'delete', class: 'btn btn-default btn-danger' %></li>
  <% else %>
    <li><%= link_to 'Login', new_user_session_path, class: 'btn btn-mini btn-success' %></li>
    <li><%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-mini btn-info' %></li>
  <% end %>
</ul>
